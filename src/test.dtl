int depth = 16;
int height = 128;
int width = 128;

for (int i = 0; i < 1400; i++)
{
    for (int j = 0; j < 32; j++)
    {
        if (isEdge? i || isEdge? j)
        {
            out = 4;
        }
        else
        {
            out = i;
        }
    }
}